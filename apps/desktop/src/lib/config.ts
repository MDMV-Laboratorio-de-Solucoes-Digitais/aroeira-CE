export const config = {
  // Theme Configuration
  theme: {
    defaultTheme: "default", // 'default' | 'aroeira' | 'enterprise'
    enableSwitcher: true,
    persist: true,
  },
  // App Metadata
  app: {
    name: "Aroeira Template",
    version: "0.1.0-alpha",
  },
};
